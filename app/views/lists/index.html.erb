<h1>All Lists</h1>

<%= link_to 'Create a New List', new_list_path, class: 'btn btn-primary my-3' %>

<ul>
  <% @lists.each do |list| %>
    <li>
      <%= link_to list.name, list_path(list) %>
    </li>
  <% end %>
</ul>
